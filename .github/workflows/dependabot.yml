version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"  # or the appropriate directory
    schedule:
      interval: "weekly"
on:
  schedule:
    - cron: "0 0 * * 0"  # Every Sunday at midnight (UTC)
